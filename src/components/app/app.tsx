import { Layout } from "../layout/layout";
import { RestaurantPage } from "../restaraunt-page/restaraunt-page";

export const App = () => {
  return (
    <Layout>
      <RestaurantPage />
    </Layout>
  );
};
