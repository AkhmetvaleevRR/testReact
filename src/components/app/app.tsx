import { Layout } from "../layout/layout";
import { RestaurantPage } from "../restaraunt-page/restaurant-page";

export const App = () => {
  return (
    <Layout>
      <RestaurantPage />
    </Layout>
  );
};
